namespace ASD.NES.Core {

    /// <summary> TV system: NTSC (US/Japan, 262 scanlines, ~60.1 Hz) or PAL (Europe, 312 scanlines, ~50 Hz). </summary>
    public enum TvRegion {
        NTSC = 0,
        PAL = 1
    }
}
